diff --git a/forge-core/src/main/java/forge/util/MyRandom.java b/forge-core/src/main/java/forge/util/MyRandom.java
index 51ade4c7e2a2eb0947cb2343646950c5972d4335..6d582011ff6576766aad1e77ff1a0e7908511d39 100644
--- a/forge-core/src/main/java/forge/util/MyRandom.java
+++ b/forge-core/src/main/java/forge/util/MyRandom.java
@@ -52,7 +52,8 @@ public class MyRandom {
      * @return the random
      */
     public static Random getRandom() {
-        return MyRandom.random;
+        ThreadUtil.ExecutionContext context = ThreadUtil.executionContext();
+        return context == null ? MyRandom.random : context.random();
     }
 
     /**
@@ -60,14 +61,16 @@ public class MyRandom {
      * @param random the random
      */
     public static void setRandom(Random random) {
-        MyRandom.random = random;
+        ThreadUtil.ExecutionContext context = ThreadUtil.executionContext();
+        if (context == null) MyRandom.random = random;
+        else context.random(random);
     }
 
     public static int[] splitIntoRandomGroups(final int value, final int numGroups) {
         int[] groups = new int[numGroups];
 
         for (int i = 0; i < value; i++) {
-            groups[random.nextInt(numGroups)]++;
+            groups[getRandom().nextInt(numGroups)]++;
         }
 
         return groups;
diff --git a/forge-core/src/main/java/forge/util/ThreadUtil.java b/forge-core/src/main/java/forge/util/ThreadUtil.java
index e06359ea4ffb20c6b92b3fc5cec06bc641f5c075..a091632522574529c3254a3fb37a0b5cb65c777b 100644
--- a/forge-core/src/main/java/forge/util/ThreadUtil.java
+++ b/forge-core/src/main/java/forge/util/ThreadUtil.java
@@ -1,8 +1,41 @@
 package forge.util;
 
 import java.util.concurrent.*;
+import java.util.Random;
+import java.util.function.Supplier;
 
 public class ThreadUtil {
+    /** Optional host-owned game scope; desktop behavior remains the fallback. */
+    public interface ExecutionContext {
+        boolean isClosed();
+        ExecutorService gameExecutor();
+        ScheduledFuture<?> schedule(int milliseconds, Runnable action);
+        Random random();
+        void random(Random value);
+        int nextId(String kind);
+        void resetId(String kind, int value);
+        <T> T cache(String kind, Supplier<T> create);
+    }
+    public interface Scope extends AutoCloseable { @Override void close(); }
+    private static final ThreadLocal<ExecutionContext> execution = new ThreadLocal<>();
+    public static ExecutionContext executionContext() { return execution.get(); }
+    public static Scope enter(ExecutionContext context) {
+        ExecutionContext previous = execution.get();
+        execution.set(context);
+        return () -> { if (previous == null) execution.remove(); else execution.set(previous); };
+    }
+    public static int nextId(String kind, java.util.concurrent.atomic.AtomicInteger fallback) {
+        ExecutionContext context = execution.get();
+        return context == null ? fallback.incrementAndGet() : context.nextId(kind);
+    }
+    public static void resetId(String kind, java.util.concurrent.atomic.AtomicInteger fallback, int value) {
+        ExecutionContext context = execution.get();
+        if (context == null) fallback.set(value); else context.resetId(kind, value);
+    }
+    public static <T> T scopedCache(String kind, T fallback, Supplier<T> create) {
+        ExecutionContext context = execution.get();
+        return context == null ? fallback : context.cache(kind, create);
+    }
     static {
         System.out.printf("(ThreadUtil first call): Running with priority %d%n", Thread.currentThread().getPriority());
     }
@@ -41,10 +74,13 @@ public class ThreadUtil {
     }
 
     public static void invokeInGameThread(Runnable toRun) {
-        getGameThreadPool().execute(toRun);
+        ExecutionContext context = execution.get();
+        (context == null ? getGameThreadPool() : context.gameExecutor()).execute(toRun);
     }
 
     public static ScheduledFuture<?> delay(int milliseconds, Runnable inputUpdater) {
+        ExecutionContext context = execution.get();
+        if (context != null) return context.schedule(milliseconds, inputUpdater);
         return getScheduledPool().schedule(inputUpdater, milliseconds, TimeUnit.MILLISECONDS);
     }
 
@@ -54,7 +90,8 @@ public class ThreadUtil {
 
     private static ExecutorService service = Executors.newWorkStealingPool();
     public static ExecutorService getServicePool() {
-        return service;
+        ExecutionContext context = execution.get();
+        return context == null ? service : context.gameExecutor();
     }
     public static void refreshServicePool() {
         service = Executors.newWorkStealingPool();
@@ -63,7 +100,7 @@ public class ThreadUtil {
         Future<T> future = null;
         T result;
         try {
-            future = service.submit(task);
+            future = getServicePool().submit(task);
             result = future.get(millis, TimeUnit.MILLISECONDS);
         } catch (Exception e) {
             result = null;
@@ -74,6 +111,7 @@ public class ThreadUtil {
         return result;
     }
     public static <T> T executeWithTimeout(Callable<T> task, int milliseconds) {
+        if (execution.get() != null) return limit(task, milliseconds);
         ExecutorService executor = Executors.newCachedThreadPool(r -> {
             Thread thread = new Thread(r);
             thread.setDaemon(true);
@@ -90,7 +128,8 @@ public class ThreadUtil {
         }
         finally {
            future.cancel(true);
+           executor.shutdownNow();
         }
         return result;
     }
-}
\ No newline at end of file
+}
diff --git a/forge-game/src/main/java/forge/game/Game.java b/forge-game/src/main/java/forge/game/Game.java
index cdc70dd5e0dd1c87bb221940666eac862f13a685..b8da49f5c0ab0160870bf6f6a3b5713813e221f4 100644
--- a/forge-game/src/main/java/forge/game/Game.java
+++ b/forge-game/src/main/java/forge/game/Game.java
@@ -64,8 +64,8 @@ public class Game {
 
     private static final TaggedLogger netLog = Logger.tag("NETWORK");
 
-    private static int maxId = 0;
-    private static int nextId() { return ++maxId; }
+    private static final java.util.concurrent.atomic.AtomicInteger maxId = new java.util.concurrent.atomic.AtomicInteger();
+    private static int nextId() { return maxId.incrementAndGet(); }
 
     private boolean noGUIUser;
 
diff --git a/forge-game/src/main/java/forge/game/GameAction.java b/forge-game/src/main/java/forge/game/GameAction.java
index aeefd9bdc3b397997a5e0622211b8e82f1216b68..b4b63b4d924377a74c7fe172c31d1cdf25529e6a 100644
--- a/forge-game/src/main/java/forge/game/GameAction.java
+++ b/forge-game/src/main/java/forge/game/GameAction.java
@@ -334,6 +334,13 @@ public class GameAction {
             ReplacementResult repres = game.getReplacementHandler().run(ReplacementType.Moved, repParams);
             copied.getOwner().removeInboundToken(copied);
 
+            // CR 800.4a: a concession during an ETB choice removed the object.
+            // Do not finish the suspended zone change and recreate that object.
+            if (!copied.getOwner().isInGame()) {
+                cleanStaticEffect(staticEff, copied);
+                return c;
+            }
+
             if (repres != ReplacementResult.NotReplaced && repres != ReplacementResult.Updated) {
                 // reset failed manifested Cards back to original
                 if ((c.isManifested() || c.isCloaked()) && !c.isInPlay()) {
diff --git a/forge-game/src/main/java/forge/game/ability/AbilityUtils.java b/forge-game/src/main/java/forge/game/ability/AbilityUtils.java
index 0e8e291c0bb807f9e5e628a4fa05d11919edd3df..c670a3ea94b6ec0489f9636a34bdae60f409e8ef 100644
--- a/forge-game/src/main/java/forge/game/ability/AbilityUtils.java
+++ b/forge-game/src/main/java/forge/game/ability/AbilityUtils.java
@@ -1300,7 +1300,17 @@ public class AbilityUtils {
         if (sa == null) {
             return;
         }
+        try {
+            resolveAvailableAbility(sa);
+        } catch (forge.game.player.PlayerController.PlayerDecisionCancelled cancelled) {
+            // Nested/repeated effects must unwind the entire removed object.
+            if (sa.getParent() != null || !cancelled.canAbandon(sa)) {
+                throw cancelled;
+            }
+        }
+    }
 
+    private static void resolveAvailableAbility(final SpellAbility sa) {
         Player pl = sa.getActivatingPlayer();
         final Game game = pl.getGame();
 
diff --git a/forge-game/src/main/java/forge/game/card/Card.java b/forge-game/src/main/java/forge/game/card/Card.java
index dcd6d517f51c8d0116afe18983a16b6a20492c36..f69d2e0e3c7a859a7bd6750e41a5e4f6d705d039 100644
--- a/forge-game/src/main/java/forge/game/card/Card.java
+++ b/forge-game/src/main/java/forge/game/card/Card.java
@@ -7460,6 +7460,7 @@ public class Card extends GameEntity implements Comparable<Card>, IHasSVars, ITr
 
     private static final Map<PaperCard, Card> cp2card = Maps.newHashMap();
     public static Card getCardForUi(IPaperCard pc) {
+        Map<PaperCard, Card> cp2card = forge.util.ThreadUtil.scopedCache("CardUI", Card.cp2card, java.util.concurrent.ConcurrentHashMap::new);
         if (pc instanceof PaperCard) {
             Card res = cp2card.get(pc);
             if (res == null) {
diff --git a/forge-game/src/main/java/forge/game/card/CounterCustomType.java b/forge-game/src/main/java/forge/game/card/CounterCustomType.java
index d9a72808a9ffe0850a9e10a273966c2c91003fcd..72fc2fc6648b04140826a7ef5d082488884ce2bb 100644
--- a/forge-game/src/main/java/forge/game/card/CounterCustomType.java
+++ b/forge-game/src/main/java/forge/game/card/CounterCustomType.java
@@ -9,7 +9,12 @@ import com.google.common.collect.Maps;
 public record CounterCustomType(String keyword) implements CounterType {
     private static Map<String, CounterCustomType> sMap = Maps.newHashMap();
 
+    private static Map<String, CounterCustomType> cache() {
+        return forge.util.ThreadUtil.scopedCache("CounterCustomType", sMap, java.util.concurrent.ConcurrentHashMap::new);
+    }
+
     public static CounterCustomType get(String s) {
+        Map<String, CounterCustomType> sMap = cache();
         if (!sMap.containsKey(s)) {
             sMap.put(s, new CounterCustomType(s));
         }
@@ -17,7 +22,7 @@ public record CounterCustomType(String keyword) implements CounterType {
     }
 
     public static Set<CounterType> getValues() {
-        return new LinkedHashSet<CounterType>(sMap.values());
+        return new LinkedHashSet<CounterType>(cache().values());
     }
     
     @Override
diff --git a/forge-game/src/main/java/forge/game/card/CounterKeywordType.java b/forge-game/src/main/java/forge/game/card/CounterKeywordType.java
index 2a4dede7490194dd5f57ea1205d307f2a090346e..b9efe9254f117a4e99c635f0d16034a3420b8eb1 100644
--- a/forge-game/src/main/java/forge/game/card/CounterKeywordType.java
+++ b/forge-game/src/main/java/forge/game/card/CounterKeywordType.java
@@ -19,7 +19,12 @@ public record CounterKeywordType(KeywordView keyword) implements CounterType {
             "Indestructible", "Lifelink", "Menace", "Reach", "Shadow", "Trample", "Vigilance");
     private static Map<String, CounterKeywordType> sMap = Maps.newHashMap();
 
+    private static Map<String, CounterKeywordType> cache() {
+        return forge.util.ThreadUtil.scopedCache("CounterKeywordType", sMap, java.util.concurrent.ConcurrentHashMap::new);
+    }
+
     public static CounterKeywordType get(String s) {
+        Map<String, CounterKeywordType> sMap = cache();
         if (!sMap.containsKey(s)) {
             sMap.put(s, new CounterKeywordType(Keyword.getInstance(s).getView()));
         }
@@ -30,7 +35,7 @@ public record CounterKeywordType(KeywordView keyword) implements CounterType {
         // add fixed first
         Set<CounterType> result = keywordCounter.stream().map(CounterKeywordType::get).collect(Collectors.toCollection(LinkedHashSet::new));
         // add variable ones later
-        result.addAll(sMap.values());
+        result.addAll(cache().values());
         return result;
     }
     
diff --git a/forge-game/src/main/java/forge/game/extrahands/BackupPlanService.java b/forge-game/src/main/java/forge/game/extrahands/BackupPlanService.java
index a61fd39d90fd8054b90294609f373aae670ebf79..e6df23bf6695b45d41550433c10ca282e8d7121d 100644
--- a/forge-game/src/main/java/forge/game/extrahands/BackupPlanService.java
+++ b/forge-game/src/main/java/forge/game/extrahands/BackupPlanService.java
@@ -73,6 +73,7 @@ public class BackupPlanService {
         }
 
         player.resetExtraZones(ZoneType.ExtraHand);
+        player.shuffle(null);
         player.updateZoneForView(player.getZone(ZoneType.Hand));
     }
 }
diff --git a/forge-game/src/main/java/forge/game/keyword/Keyword.java b/forge-game/src/main/java/forge/game/keyword/Keyword.java
index 646f3131b54c73e4286a8c4b635761a9d9f94cab..06bfe1036c063656a50d93e1103ad29ef61aaef7 100644
--- a/forge-game/src/main/java/forge/game/keyword/Keyword.java
+++ b/forge-game/src/main/java/forge/game/keyword/Keyword.java
@@ -292,7 +292,7 @@ public enum Keyword {
         return getKeywordDetails(k).getKey();
     }
 
-    private static final Map<String, Set<Keyword>> cardKeywordSetLookup = new HashMap<>();
+    private static final Map<String, Set<Keyword>> cardKeywordSetLookup = new java.util.concurrent.ConcurrentHashMap<>();
 
     public static Set<Keyword> getKeywordSet(PaperCard card) {
         String name = card.getName();
diff --git a/forge-game/src/main/java/forge/game/phase/PhaseHandler.java b/forge-game/src/main/java/forge/game/phase/PhaseHandler.java
index 838c73f239a290cea8525d36ee281480f28d44fb..8b8f44751f32106d715e2f5a1907a4c73fa14cce 100644
--- a/forge-game/src/main/java/forge/game/phase/PhaseHandler.java
+++ b/forge-game/src/main/java/forge/game/phase/PhaseHandler.java
@@ -1051,17 +1051,21 @@ public class PhaseHandler implements java.io.Serializable, IHasForgeLog {
                     // state-based effects check could lead to game over
                     return;
                 }
+                // CR 800.4a/j: never request priority from a player who left
+                // during resolution or a state-based action.
+                if (!pPlayerPriority.isInGame()) {
+                    Player next = game.getNextPlayerAfter(pPlayerPriority);
+                    if (next == null) { return; }
+                    pPlayerPriority = next;
+                    if (!pFirstPriority.isInGame()) { pFirstPriority = next; }
+                }
                 game.stashGameState();
 
                 chosenSa = pPlayerPriority.getController().chooseSpellAbilityToPlay();
 
-                // this needs to come after chosenSa so it sees you conceding on own turn
-                if (playerTurn.hasLost() && pPlayerPriority.equals(playerTurn) && pFirstPriority.equals(playerTurn)) {
-                    // If the active player has lost, and they have priority, set the next player to have priority
-                    System.out.println("Active player is no longer in the game...");
-                    pPlayerPriority = game.getNextPlayerAfter(getPriorityPlayer());
-                    pFirstPriority = pPlayerPriority;
-                }
+                // A concession while this input was open is not a pass by
+                // its successor. Ask that successor before advancing priority.
+                if (!pPlayerPriority.isInGame()) { continue; }
 
                 if (chosenSa == null) {
                     break; // that means 'I pass'
diff --git a/forge-game/src/main/java/forge/game/player/PlaySpellAbility.java b/forge-game/src/main/java/forge/game/player/PlaySpellAbility.java
index fa902c5081b7d7e8cb06ffa1ce53ce338a7829c6..c23610f8725190bb2c5f4b24509c032c1f47e4d8 100644
--- a/forge-game/src/main/java/forge/game/player/PlaySpellAbility.java
+++ b/forge-game/src/main/java/forge/game/player/PlaySpellAbility.java
@@ -17,6 +17,8 @@
  */
 package forge.game.player;
 
+import forge.game.player.PlayerController.PlayerDecisionCancelled;
+
 import com.google.common.collect.Iterables;
 import forge.card.CardStateName;
 import forge.card.CardType;
@@ -118,6 +120,9 @@ public class PlaySpellAbility {
 
         final PlaySpellAbility req = new PlaySpellAbility(controller, sa);
         if (!req.playAbility(true, false, false)) {
+            if (!p.isInGame() || !source.getOwner().isInGame()) {
+                return false;
+            }
             if (!controller.getGame().EXPERIMENTAL_RESTORE_SNAPSHOT) {
                 Card rollback = p.getGame().getCardState(source);
                 if (castFaceDown) {
@@ -579,6 +584,27 @@ public class PlaySpellAbility {
     }
 
     public final boolean playAbility(final boolean mayChooseTargets, final boolean isFree, final boolean skipStack) {
+        final Player player = ability.getActivatingPlayer();
+        final Game game = player.getGame();
+        final boolean wasFrozen = game.getStack().isFrozen();
+        try {
+            return playAvailableAbility(mayChooseTargets, isFree, skipStack);
+        } catch (PlayerDecisionCancelled cancelled) {
+            if (!cancelled.canAbandon(ability)) {
+                throw cancelled;
+            }
+            // Their cards already left under CR 800.4a; rolling the cast back
+            // would recreate them. Only release the native transaction state.
+            game.clearTopLibsCast(ability);
+            if (!wasFrozen) {
+                game.getStack().unfreezeStack();
+            }
+            player.getManaPool().restoreColorReplacements();
+            return false;
+        }
+    }
+
+    private boolean playAvailableAbility(final boolean mayChooseTargets, final boolean isFree, final boolean skipStack) {
         final Player player = ability.getActivatingPlayer();
         final Game game = player.getGame();
         boolean refreeze = game.getStack().isFrozen();
diff --git a/forge-game/src/main/java/forge/game/player/PlayerController.java b/forge-game/src/main/java/forge/game/player/PlayerController.java
index 192ef8737ecd645b75d50782693d5eab948d43a7..3260ed5850a4d0099a43224b01504c12de9f5fa2 100644
--- a/forge-game/src/main/java/forge/game/player/PlayerController.java
+++ b/forge-game/src/main/java/forge/game/player/PlayerController.java
@@ -49,6 +49,56 @@ import java.util.stream.Collectors;
  */
 public abstract class PlayerController {
 
+    /** Abandons a decision only after its player and controlling object left the game. */
+    public static final class PlayerDecisionCancelled extends java.util.concurrent.CancellationException {
+        private final Player player;
+        private final SpellAbility ability;
+
+        public PlayerDecisionCancelled(Player player, SpellAbility ability) {
+            super("The deciding player left the game");
+            if (player == null || player.isInGame() || ability == null) {
+                throw new IllegalArgumentException("Only a departed player's object decision can be abandoned");
+            }
+            this.player = player;
+            this.ability = ability;
+        }
+
+        public boolean canAbandon(SpellAbility current) {
+            if (player.isInGame() || current == null || current.getHostCard().getGame() != player.getGame()) {
+                return false;
+            }
+            // CR 800.4a removes every spell/ability controlled by the departing player.
+            // Replacement abilities of their departed permanents cease with the object.
+            return ability.getActivatingPlayer() == player && current.getActivatingPlayer() == player
+                || ability.getRootAbility().isSpell() && ability.getHostCard().getOwner() == player
+                    && current.getRootAbility() == ability.getRootAbility()
+                || ability == current && current.isReplacementAbility() && current.getHostCard().getOwner() == player;
+        }
+    }
+
+    /** CR 800.4g: the object's controller explicitly chooses the replacement. */
+    public final Player choosePlayerForDepartedDecision(SpellAbility ability, Player departed) {
+        if (departed.isInGame() || !player.isInGame() || ability.getActivatingPlayer() != player) {
+            throw new IllegalArgumentException("A departed player's choice requires its surviving object controller");
+        }
+        while (player.isInGame()) {
+            forge.util.collect.FCollection<Player> choices = new forge.util.collect.FCollection<>(getGame().getPlayers());
+            if (departed.isOpponentOf(player)) {
+                forge.util.collect.FCollection<Player> opponents = new forge.util.collect.FCollection<>();
+                for (Player candidate : choices) {
+                    if (candidate.isOpponentOf(player)) { opponents.add(candidate); }
+                }
+                if (!opponents.isEmpty()) { choices = opponents; }
+            }
+            Player chosen = chooseSingleEntityForEffect(choices, ability,
+                    "Choose a player to make the departed player's choice", Map.of());
+            if (player.isInGame() && chosen != null && chosen.isInGame()) { return chosen; }
+            // A candidate can leave while the native choice is open. Ask again
+            // from the surviving set rather than continuing with that player.
+        }
+        throw new PlayerDecisionCancelled(player, ability);
+    }
+
     public enum BinaryChoiceType {
         HeadsOrTails, // coin
         TapOrUntap,
diff --git a/forge-game/src/main/java/forge/game/replacement/ReplacementEffect.java b/forge-game/src/main/java/forge/game/replacement/ReplacementEffect.java
index b5b396dc82f9928041f0486277fcf4944b538315..4855d00267ca194d7d513ef3f653d03ea69b68a3 100644
--- a/forge-game/src/main/java/forge/game/replacement/ReplacementEffect.java
+++ b/forge-game/src/main/java/forge/game/replacement/ReplacementEffect.java
@@ -47,8 +47,8 @@ import forge.util.TextUtil;
  *
  */
 public abstract class ReplacementEffect extends TriggerReplacementBase {
-    private static int maxId = 0;
-    private static int nextId() { return ++maxId; }
+    private static final java.util.concurrent.atomic.AtomicInteger maxId = new java.util.concurrent.atomic.AtomicInteger();
+    private static int nextId() { return forge.util.ThreadUtil.nextId("ReplacementEffect", maxId); }
 
     /** The ID. */
     private int id;
diff --git a/forge-game/src/main/java/forge/game/spellability/SpellAbility.java b/forge-game/src/main/java/forge/game/spellability/SpellAbility.java
index da6f782e3f8cb7c3f07955c587ba3401be330e38..13fb3efae8dc6bc32d9def701ff997fcd17ab13c 100644
--- a/forge-game/src/main/java/forge/game/spellability/SpellAbility.java
+++ b/forge-game/src/main/java/forge/game/spellability/SpellAbility.java
@@ -75,8 +75,8 @@ import forge.game.zone.ZoneType;
  * @version $Id$
  */
 public abstract class SpellAbility extends CardTraitBase implements ISpellAbility, IIdentifiable, Comparable<SpellAbility> {
-    private static int maxId = 0;
-    private static int nextId() { return ++maxId; }
+    private static final java.util.concurrent.atomic.AtomicInteger maxId = new java.util.concurrent.atomic.AtomicInteger();
+    private static int nextId() { return forge.util.ThreadUtil.nextId("SpellAbility", maxId); }
 
     public static class EmptySa extends SpellAbility {
         public EmptySa(Card sourceCard) { super(sourceCard, Cost.Zero); setActivatingPlayer(sourceCard.getController());}
diff --git a/forge-game/src/main/java/forge/game/spellability/SpellAbilityStackInstance.java b/forge-game/src/main/java/forge/game/spellability/SpellAbilityStackInstance.java
index f67dd5b4e88bb898a7a5db2b9d6ffe138d6b9e49..ff8bb2d7468402c2c8afe2fcf9f5ceb48ca14daf 100644
--- a/forge-game/src/main/java/forge/game/spellability/SpellAbilityStackInstance.java
+++ b/forge-game/src/main/java/forge/game/spellability/SpellAbilityStackInstance.java
@@ -38,8 +38,8 @@ import forge.util.TextUtil;
  * @version $Id$
  */
 public class SpellAbilityStackInstance implements IIdentifiable, IHasCardView {
-    private static int maxId = 0;
-    public static int nextId() { return ++maxId; }
+    private static final java.util.concurrent.atomic.AtomicInteger maxId = new java.util.concurrent.atomic.AtomicInteger();
+    public static int nextId() { return forge.util.ThreadUtil.nextId("SpellAbilityStackInstance", maxId); }
 
     // At some point I want this functioning more like Target/Target Choices
     // where the SA has an "active"
diff --git a/forge-game/src/main/java/forge/game/staticability/StaticAbility.java b/forge-game/src/main/java/forge/game/staticability/StaticAbility.java
index 229e244e7a4568cfe9cdc1c7dda202cc1375e941..3868ce4fb44603601ea7d3865eb2d140651e05ba 100644
--- a/forge-game/src/main/java/forge/game/staticability/StaticAbility.java
+++ b/forge-game/src/main/java/forge/game/staticability/StaticAbility.java
@@ -51,8 +51,8 @@ import forge.util.*;
  * The Class StaticAbility.
  */
 public class StaticAbility extends CardTraitBase implements IIdentifiable, Cloneable, Comparable<StaticAbility> {
-    private static int maxId = 0;
-    private static int nextId() { return ++maxId; }
+    private static final java.util.concurrent.atomic.AtomicInteger maxId = new java.util.concurrent.atomic.AtomicInteger();
+    private static int nextId() { return forge.util.ThreadUtil.nextId("StaticAbility", maxId); }
 
     private int id;
 
diff --git a/forge-game/src/main/java/forge/game/trigger/Trigger.java b/forge-game/src/main/java/forge/game/trigger/Trigger.java
index 186d4cdf4c60dd411811cb84fcf8027db2c23095..af60676492debab553e3214e3dd8233b07414296 100644
--- a/forge-game/src/main/java/forge/game/trigger/Trigger.java
+++ b/forge-game/src/main/java/forge/game/trigger/Trigger.java
@@ -52,8 +52,8 @@ import java.util.*;
  * @version $Id$
  */
 public abstract class Trigger extends TriggerReplacementBase {
-    private static int maxId = 0;
-    private static int nextId() { return ++maxId; }
+    private static final java.util.concurrent.atomic.AtomicInteger maxId = new java.util.concurrent.atomic.AtomicInteger();
+    private static int nextId() { return forge.util.ThreadUtil.nextId("Trigger", maxId); }
 
     /**
      * <p>
@@ -61,7 +61,7 @@ public abstract class Trigger extends TriggerReplacementBase {
      * </p>
      */
     public static void resetIDs() {
-        Trigger.maxId = 50000;
+        forge.util.ThreadUtil.resetId("Trigger", maxId, 50000);
     }
 
     /** The ID. */
diff --git a/forge-game/src/main/java/forge/game/zone/MagicStack.java b/forge-game/src/main/java/forge/game/zone/MagicStack.java
index 35ea040cc89fd8105635f3426031a44bfaa117c5..ef68d3bb09c7b836f526d165a77c1f5d0544f732 100644
--- a/forge-game/src/main/java/forge/game/zone/MagicStack.java
+++ b/forge-game/src/main/java/forge/game/zone/MagicStack.java
@@ -615,6 +615,20 @@ public class MagicStack /* extends MyObservable */ implements Iterable<SpellAbil
         } else if (sa.getApi() != null) {
             AbilityUtils.handleRemembering(sa);
             AbilityUtils.resolve(sa);
+            if (getInstanceMatchingSpellAbilityID(sa) == null
+                    && (!sa.getActivatingPlayer().isInGame() || !source.getOwner().isInGame())) {
+                // A concession removed this object during a synchronous choice.
+                // Release resolution state without publishing a fabricated resolution.
+                if (sa.getControlledByPlayer() != null) {
+                    sa.getActivatingPlayer().removeController(sa.getControlledByPlayer().getLeft());
+                    sa.setControlledByPlayer(-1, null);
+                    sa.setManaCostBeingPaid(null);
+                }
+                game.getAction().checkStaticAbilities();
+                finishResolving(sa, false);
+                game.copyLastState();
+                return;
+            }
             final Map<AbilityKey, Object> runParams = AbilityKey.mapFromCard(source);
             runParams.put(AbilityKey.SpellAbility, sa);
             game.getTriggerHandler().runTrigger(TriggerType.AbilityResolves, runParams, false);
diff --git a/forge-game/src/main/java/forge/trackable/TrackableTypes.java b/forge-game/src/main/java/forge/trackable/TrackableTypes.java
index a6cf5f1521e668b160713ead14aa16096aaa8c3c..9d05fe8a973eb0f15a490f3a26f00fac2ca9d1b5 100644
--- a/forge-game/src/main/java/forge/trackable/TrackableTypes.java
+++ b/forge-game/src/main/java/forge/trackable/TrackableTypes.java
@@ -4,7 +4,6 @@ import java.util.List;
 import java.util.Map;
 import java.util.Set;
 
-import com.google.common.collect.Maps;
 import com.google.common.collect.Multiset;
 
 import forge.card.CardType;
@@ -136,7 +135,7 @@ public class TrackableTypes {
     public static final TrackableType<Object> ObjectType = new TrackableType<Object>(null);
 
     //make this quicker than having to define a new class for every single enum
-    private static Map<Class<? extends Enum<?>>, TrackableType<?>> enumTypes = Maps.newHashMap();
+    private static Map<Class<? extends Enum<?>>, TrackableType<?>> enumTypes = new java.util.concurrent.ConcurrentHashMap<>();
 
     @SuppressWarnings("unchecked")
     public static <E extends Enum<E>> TrackableType<E> EnumType(final Class<E> enumType) {
diff --git a/forge-gui/src/main/java/forge/gamemodes/match/input/InputLockUI.java b/forge-gui/src/main/java/forge/gamemodes/match/input/InputLockUI.java
index 65e0ae93ef577d1ca25793c12e5238ea3e812523..bdc586619c680b8c1c12571d4dd9eb5e94615f34 100644
--- a/forge-gui/src/main/java/forge/gamemodes/match/input/InputLockUI.java
+++ b/forge-gui/src/main/java/forge/gamemodes/match/input/InputLockUI.java
@@ -11,10 +11,12 @@ import forge.util.Localizer;
 import forge.util.ThreadUtil;
 
 import java.util.List;
+import java.util.concurrent.ScheduledFuture;
 import java.util.concurrent.atomic.AtomicInteger;
 
 public class InputLockUI implements Input {
     private final AtomicInteger iCall = new AtomicInteger();
+    private ScheduledFuture<?> pendingUpdate;
 
     private final InputQueue inputQueue;
     private final PlayerControllerHuman controller;
@@ -29,9 +31,12 @@ public class InputLockUI implements Input {
     }
 
     @Override
-    public void showMessageInitial() {
+    public synchronized void showMessageInitial() {
         final int ixCall = 1 + iCall.getAndIncrement();
-        ThreadUtil.delay(500, new InputUpdater(ixCall));
+        // Superseded waiting-message refreshes have no useful work left. Do
+        // not retain one timer per priority transition during rapid passing.
+        if (pendingUpdate != null) pendingUpdate.cancel(false);
+        pendingUpdate = ThreadUtil.delay(500, new InputUpdater(ixCall));
     }
 
     @Override
diff --git a/forge-gui/src/main/java/forge/gamemodes/match/input/InputLondonMulligan.java b/forge-gui/src/main/java/forge/gamemodes/match/input/InputLondonMulligan.java
index 5932059d5d752a06a1e817a921067a7fac779f34..a5bb20155d86c0190fbf90c709f44102c3f2ad8f 100644
--- a/forge-gui/src/main/java/forge/gamemodes/match/input/InputLondonMulligan.java
+++ b/forge-gui/src/main/java/forge/gamemodes/match/input/InputLondonMulligan.java
@@ -125,6 +125,9 @@ public class InputLondonMulligan extends InputSyncronizedBase {
         return true;
     }
 
+    /** Number of cards required by the native London mulligan. */
+    public int getCardsToReturn() { return toReturn; }
+
     public CardCollectionView getSelectedCards() {
         return selected;
     }
diff --git a/forge-gui/src/main/java/forge/gamemodes/match/input/InputPayMana.java b/forge-gui/src/main/java/forge/gamemodes/match/input/InputPayMana.java
index 7350155fab629d2bb38a71c032599258a2c53c5b..29f470e35e76689a0370bb9fa682beb62033b2bc 100644
--- a/forge-gui/src/main/java/forge/gamemodes/match/input/InputPayMana.java
+++ b/forge-gui/src/main/java/forge/gamemodes/match/input/InputPayMana.java
@@ -48,7 +48,10 @@ public abstract class InputPayMana extends InputSyncronizedBase {
     private CardCollection autoPayManaSources = null;
     private boolean autoPayManaSourcesKnown = false;
 
-    private boolean locked = false;
+    private volatile boolean locked = false;
+    // A nested payment can restore this input before the mana ability has
+    // produced and applied its mana. UI refreshes must not release this guard.
+    private volatile boolean resolvingManaAction = false;
 
     protected InputPayMana(final PlayerControllerHuman controller, final SpellAbility saPaidFor0, final Player player0, final boolean effect) {
         super(controller);
@@ -119,7 +122,7 @@ public abstract class InputPayMana extends InputSyncronizedBase {
         final Collection<SpellAbility> toRemove = Lists.newArrayListWithCapacity(result.size());
         for (final SpellAbility sa : result) {
             sa.setActivatingPlayer(player);
-            if (sa.canPlay(true)) {
+            if (sa.canPlay(true) && sa.checkRestrictions(player)) {
                 continue;
             }
             toRemove.add(sa);
@@ -207,7 +210,7 @@ public abstract class InputPayMana extends InputSyncronizedBase {
         return activateManaAbility(card, null);
     }
     protected boolean activateManaAbility(final Card card, SpellAbility chosenAbility) {
-        if (locked) {
+        if (locked || resolvingManaAction) {
             System.err.print("Should wait till previous call to playAbility finishes.");
             return false;
         }
@@ -329,7 +332,9 @@ public abstract class InputPayMana extends InputSyncronizedBase {
         // System.out.println("Chosen sa=" + chosen + " of " + chosen.getHostCard() + " to pay mana");
 
         locked = true;
+        resolvingManaAction = true;
         game.getAction().invoke(() -> {
+            try {
             if (PlaySpellAbility.playSpellAbility(getController(), chosen.getActivatingPlayer(), chosen)) {
                 final List<AbilityManaPart> manaAbilities = chosen.getAllManaParts();
                 boolean restrictionsMet = true;
@@ -350,6 +355,9 @@ public abstract class InputPayMana extends InputSyncronizedBase {
                     invalidateAutoPayManaSources();
                 }
             }
+            } finally {
+                resolvingManaAction = false;
+            }
             // Need to call this to unlock
             onStateChanged();
         });
@@ -357,6 +365,11 @@ public abstract class InputPayMana extends InputSyncronizedBase {
         return true;
     }
 
+    /** Read-only metadata for remote renderers of this native payment input. */
+    public final String getUnpaidManaCost() { return manaCost.toString(); }
+    public final boolean isPaymentActionPending() { return locked || resolvingManaAction; }
+    public final boolean supportsAutomaticPayment() { return supportAutoPay(); }
+
     protected boolean isAlreadyPaid() {
         if (manaCost.isPaid()) {
             bPaid = true;
@@ -374,13 +387,18 @@ public abstract class InputPayMana extends InputSyncronizedBase {
 
     @Override
     protected void onOk() {
-        if (supportAutoPay() && !locked) { //prevent AI taking over from double-clicking Auto
+        if (supportAutoPay() && !locked && !resolvingManaAction) { //prevent AI taking over from double-clicking Auto
             locked = true;
+            resolvingManaAction = true;
             //use AI utility to automatically pay mana cost if possible
             final Runnable proc = () -> ComputerUtilMana.payManaCost(manaCost, saPaidFor, player, effect);
             //must run in game thread as certain payment actions can only be automated there
             game.getAction().invoke(() -> {
-                runAsAi(proc);
+                try {
+                    runAsAi(proc);
+                } finally {
+                    resolvingManaAction = false;
+                }
                 onStateChanged();
             });
         }
diff --git a/forge-gui/src/main/java/forge/gamemodes/match/input/InputPayManaOfCostPayment.java b/forge-gui/src/main/java/forge/gamemodes/match/input/InputPayManaOfCostPayment.java
index 73926b484fe4aaf3905348a945d9c67cacc37fe1..3de28a856190d8146b78561db8201ed98f2fe838 100644
--- a/forge-gui/src/main/java/forge/gamemodes/match/input/InputPayManaOfCostPayment.java
+++ b/forge-gui/src/main/java/forge/gamemodes/match/input/InputPayManaOfCostPayment.java
@@ -40,10 +40,17 @@ public class InputPayManaOfCostPayment extends InputPayMana {
     private static final long serialVersionUID = 3467312982164195091L;
     private ManaConversionMatrix extraMatrix;
 
+    /** Whether the native life-total click can pay one remaining mana symbol. */
+    public final boolean canPayLifeForMana() {
+        return player.canPayLife(this.phyLifeToLose + 2, this.effect, saPaidFor)
+                && (manaCost.containsPhyrexianMana()
+                || player.hasKeyword("PayLifeInsteadOf:B") && manaCost.hasAnyKind(ManaAtom.BLACK));
+    }
+
     @Override
     protected final void onPlayerSelected(Player selected, final ITriggerEvent triggerEvent) {
         if (player == selected) {
-            if (player.canPayLife(this.phyLifeToLose + 2, this.effect, saPaidFor)) {
+            if (canPayLifeForMana()) {
                 if (manaCost.payPhyrexian()) {
                     saPaidFor.setSpendPhyrexianMana(true);
                     this.phyLifeToLose += 2;
diff --git a/forge-gui/src/main/java/forge/gamemodes/match/input/InputQueue.java b/forge-gui/src/main/java/forge/gamemodes/match/input/InputQueue.java
index 32dafaf9e2da45da0ac294ad1fa9eb66a1912de6..5fd7da4a2bef067f9a21a0e0ac766221c2d5c81a 100644
--- a/forge-gui/src/main/java/forge/gamemodes/match/input/InputQueue.java
+++ b/forge-gui/src/main/java/forge/gamemodes/match/input/InputQueue.java
@@ -37,6 +37,7 @@ public class InputQueue extends Observable implements IHasForgeLog {
 
     private final BlockingDeque<InputSynchronized> inputStack = new LinkedBlockingDeque<>();
     private final GameView gameView;
+    private InputLockUI lockInput;
 
     public InputQueue(final GameView gameView, final InputProxy inputProxy) {
         this.gameView = gameView;
@@ -79,12 +80,15 @@ public class InputQueue extends Observable implements IHasForgeLog {
         updateObservers();
     }
 
-    public final Input getActualInput(final PlayerControllerHuman controller) {
+    public final synchronized Input getActualInput(final PlayerControllerHuman controller) {
         final Input topMost = inputStack.peek(); // incoming input to Control
         if (topMost != null && !gameView.isGameOver()) {
             return topMost;
         }
-        return new InputLockUI(this, controller);
+        // Each controller owns one queue. Reuse its waiting input so newer
+        // refreshes can cancel the previous input's delayed presentation task.
+        if (lockInput == null) lockInput = new InputLockUI(this, controller);
+        return lockInput;
     } // getInput()
 
     // only for debug purposes
diff --git a/forge-gui/src/main/java/forge/gamemodes/match/input/InputSelectCardsForConvokeOrImprovise.java b/forge-gui/src/main/java/forge/gamemodes/match/input/InputSelectCardsForConvokeOrImprovise.java
index 39afa242b1e681dc1d7a30eba0eacbaa19313775..e4904d9e63c7202f87b59be752a0000c20a847ff 100644
--- a/forge-gui/src/main/java/forge/gamemodes/match/input/InputSelectCardsForConvokeOrImprovise.java
+++ b/forge-gui/src/main/java/forge/gamemodes/match/input/InputSelectCardsForConvokeOrImprovise.java
@@ -133,6 +133,9 @@ public final class InputSelectCardsForConvokeOrImprovise extends InputSelectMany
         return null;
     }
 
+    /** Candidates already selected by the native cost adjustment. */
+    public CardCollectionView getAvailableCards() { return availableCards; }
+
     public Map<Card, ManaCostShard> getConvokeMap() {
         if (hasCancelled()) {
             return Maps.newHashMap();
diff --git a/forge-gui/src/main/java/forge/gamemodes/match/input/InputSelectTargets.java b/forge-gui/src/main/java/forge/gamemodes/match/input/InputSelectTargets.java
index ca152203513a86cc725dbdc9ce002ada74d1f595..63a632180a364cb0025dc28895eb22e778d6fae9 100644
--- a/forge-gui/src/main/java/forge/gamemodes/match/input/InputSelectTargets.java
+++ b/forge-gui/src/main/java/forge/gamemodes/match/input/InputSelectTargets.java
@@ -44,6 +44,22 @@ public final class InputSelectTargets extends InputSyncronizedBase {
     private boolean mustTargetFiltered;
     private static final long serialVersionUID = -1091595663541356356L;
 
+    /** Card candidates already supplied by the native target setup. */
+    public List<Card> getSelectableCards() { return java.util.Collections.unmodifiableList(choices); }
+
+    /** Uses the same native player-target predicates as the input action. */
+    public List<Player> getSelectablePlayers() {
+        return getController().getGame().getPlayers().stream()
+                .filter(p -> targets.contains(p) || canSelectPlayer(p)).toList();
+    }
+
+    private boolean canSelectPlayer(final Player player) {
+        return !player.hasLost()
+                && !(sa.isSpell() && sa.getHostCard().isAura() && !player.canBeAttached(sa.getHostCard(), sa))
+                && sa.canTarget(player) && !mustTargetFiltered
+                && (filter == null || filter.test(player));
+    }
+
     public boolean hasCancelled() { return bCancel; }
     public boolean hasPressedOk() { return bOk; }
 
@@ -337,21 +353,7 @@ public final class InputSelectTargets extends InputSyncronizedBase {
             return;
         }
 
-        if (player.hasLost()) {
-            showMessage(sa.getHostCard() + " - Cannot target this player - already lost.");
-            return;
-        }
-
-        //TODO return the correct reason to display
-        if (sa.isSpell() && sa.getHostCard().isAura() && !player.canBeAttached(sa.getHostCard(), sa)) {
-            showMessage(sa.getHostCard() + " - Cannot enchant this player (Hexproof? Protection? Restrictions?).");
-            return;
-        }
-        if (!sa.canTarget(player) || mustTargetFiltered) {
-            showMessage(sa.getHostCard() + " - Cannot target this player (Hexproof? Protection? Restrictions?).");
-            return;
-        }
-        if (filter != null && !filter.test(player)) {
+        if (!canSelectPlayer(player)) {
             showMessage(sa.getHostCard() + " - Cannot target this player (Hexproof? Protection? Restrictions?).");
             return;
         }
diff --git a/forge-gui/src/main/java/forge/gamemodes/match/input/InputSyncronizedBase.java b/forge-gui/src/main/java/forge/gamemodes/match/input/InputSyncronizedBase.java
index 014cb5db0f6986ef9641f823784c2a42840a993f..bdbe10e5a9997b112d025f65d972f57bf5d98533 100644
--- a/forge-gui/src/main/java/forge/gamemodes/match/input/InputSyncronizedBase.java
+++ b/forge-gui/src/main/java/forge/gamemodes/match/input/InputSyncronizedBase.java
@@ -1,11 +1,13 @@
 package forge.gamemodes.match.input;
 
 import forge.util.IHasForgeLog;
+import forge.util.ThreadUtil;
 import forge.gui.FThreads;
 import forge.gui.error.BugReporter;
 import forge.player.PlayerControllerHuman;
 
 import java.util.concurrent.CountDownLatch;
+import java.util.concurrent.CancellationException;
 
 public abstract class InputSyncronizedBase extends InputBase implements InputSynchronized, IHasForgeLog {
 
@@ -17,13 +19,24 @@ public abstract class InputSyncronizedBase extends InputBase implements InputSyn
         cdlDone = new CountDownLatch(1);
     }
 
+    /** True while this input's native game-thread wait has not been released. */
+    public final boolean isAwaitingInput() {
+        return cdlDone.getCount() != 0;
+    }
+
     @Override
     public void awaitLatchRelease() {
+        ThreadUtil.ExecutionContext context = ThreadUtil.executionContext();
+        if (context != null && context.isClosed()) throw new CancellationException("Game execution was closed");
         FThreads.assertExecutedByEdt(false);
         netLog.trace("awaitLatchRelease() starting on {}, thread = {}", this.getClass().getSimpleName(), Thread.currentThread().getName());
         try {
             cdlDone.await();
         } catch (final InterruptedException e) {
+            if (context != null) {
+                Thread.currentThread().interrupt();
+                throw new CancellationException("Game input wait was interrupted");
+            }
             BugReporter.reportException(e);
         }
         netLog.trace("awaitLatchRelease() UNBLOCKED on {}, thread = {}", this.getClass().getSimpleName(), Thread.currentThread().getName());
diff --git a/forge-gui/src/main/java/forge/localinstance/properties/ForgeProfileProperties.java b/forge-gui/src/main/java/forge/localinstance/properties/ForgeProfileProperties.java
index dac033fdecf85bfba545db44f66a615e268418f4..370bbf60c5f77913b5e68649a772b275010a3a84 100644
--- a/forge-gui/src/main/java/forge/localinstance/properties/ForgeProfileProperties.java
+++ b/forge-gui/src/main/java/forge/localinstance/properties/ForgeProfileProperties.java
@@ -59,8 +59,14 @@ public class ForgeProfileProperties {
     public static void load(boolean isUsingAppDirectory) {
         final Properties props = new Properties();
         final File propFile = new File(ForgeConstants.PROFILE_FILE);
+        // A remote host may isolate each game process without changing the
+        // shared assets profile or relying on platform-specific home variables.
+        final String processProfile = System.getProperty("forge.processProfile");
         try {
-            if (propFile.canRead() && !isUsingAppDirectory) {
+            if (processProfile != null) {
+                props.setProperty(USER_DIR_KEY, new File(processProfile, "data").getAbsolutePath());
+                props.setProperty(CACHE_DIR_KEY, new File(processProfile, "cache").getAbsolutePath());
+            } else if (propFile.canRead() && !isUsingAppDirectory) {
                 props.load(Files.newInputStream(propFile.toPath()));
             }
         } catch (final IOException e) {
@@ -204,6 +210,9 @@ public class ForgeProfileProperties {
     }
 
     private static void save() {
+        if (System.getProperty("forge.processProfile") != null) {
+            return; // Process-local paths must never modify shared assets.
+        }
         final Pair<String, String> defaults = getDefaultDirs();
         final String defaultUserDir = defaults.getLeft() + File.separator;
         final String defaultDecksDir = defaultUserDir + "decks" + File.separator;
