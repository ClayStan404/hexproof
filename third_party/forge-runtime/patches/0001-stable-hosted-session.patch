diff --git a/forge-harness/pom.xml b/forge-harness/pom.xml
index b325d9f27c876bb824192cdd036a1b2e3c810741..76841d8d0a98979ea115fe64465fbb1fd3c03863 100644
--- a/forge-harness/pom.xml
+++ b/forge-harness/pom.xml
@@ -36,6 +36,17 @@
             <groupId>forge</groupId>
             <artifactId>forge-gui</artifactId>
             <version>${project.version}</version>
+            <exclusions>
+                <exclusion>
+                    <groupId>xmlpull</groupId>
+                    <artifactId>xmlpull</artifactId>
+                </exclusion>
+            </exclusions>
+        </dependency>
+        <dependency>
+            <groupId>forge</groupId>
+            <artifactId>hexproof-xmlpull-api</artifactId>
+            <version>${project.version}</version>
         </dependency>
         <dependency>
             <groupId>com.google.code.gson</groupId>
diff --git a/forge-harness/src/main/java/forge/harness/host/InteractiveSnapshotExtractor.java b/forge-harness/src/main/java/forge/harness/host/InteractiveSnapshotExtractor.java
index 2993bd8f5136e6d2e656fc816393b5903deb9ad8..503e411bfbc57d4b84ceed33103a82a8e953c341 100644
--- a/forge-harness/src/main/java/forge/harness/host/InteractiveSnapshotExtractor.java
+++ b/forge-harness/src/main/java/forge/harness/host/InteractiveSnapshotExtractor.java
@@ -198,6 +198,10 @@ public final class InteractiveSnapshotExtractor {
         view.put("step", normalizeStep((String) base.get("phase")));
         view.put("combatAssignments", snapshotCombat(game));
         view.put("activePlayerId", activePlayerId);
+        if (game.getStartingPlayer() != null) {
+            view.put("startingPlayerId", "player-"
+                    + SnapshotExtractor.playerIndex(game, game.getStartingPlayer()));
+        }
         view.put("priorityPlayerId", "player-" + asIndex(base.get("priority_player")));
         view.put("players", players);
         view.put("zones", zones);
@@ -921,6 +925,7 @@ public final class InteractiveSnapshotExtractor {
                     && (source.isDoubleFaced() || source.isModal()));
             stackItem.put("faceIndex", stackFaceIndex(source, sa));
             stackItem.put("targets", stackTargets(game, item.getSpellAbility()));
+            redactFaceDownStack(stackItem, source);
             out.add(stackItem);
             index++;
         }
@@ -968,9 +973,23 @@ public final class InteractiveSnapshotExtractor {
         stackItem.put("isDoubleFaced", source.isDoubleFaced() || source.isModal());
         stackItem.put("faceIndex", stackFaceIndex(source, castingAbility));
         stackItem.put("targets", stackTargets(game, castingAbility));
+        redactFaceDownStack(stackItem, source);
         return stackItem;
     }
 
+    private static void redactFaceDownStack(final Map<String, Object> item, final Card source) {
+        if (source == null || !source.isFaceDown()) {
+            return;
+        }
+        // A paper printing and ability description still identify a morph even
+        // when its face-down name is empty. The stack is public for all viewers.
+        item.put("identity", stackIdentity("", null));
+        item.put("text", "Face-down spell");
+        item.remove("sourceAbilityText");
+        item.put("isDoubleFaced", false);
+        item.put("faceIndex", 0);
+    }
+
     private static String sourceAbilityText(final SpellAbility ability) {
         if (ability == null) {
             return null;
diff --git a/forge-harness/src/main/java/forge/harness/host/ManaBrewEngineAdapter.java b/forge-harness/src/main/java/forge/harness/host/ManaBrewEngineAdapter.java
index 33fefe4a59868da82a8ceb5a12e81a32165e71ca..42f3fee5036f511334f64e3e3636a8e0e9b40b12 100644
--- a/forge-harness/src/main/java/forge/harness/host/ManaBrewEngineAdapter.java
+++ b/forge-harness/src/main/java/forge/harness/host/ManaBrewEngineAdapter.java
@@ -97,11 +97,15 @@ public final class ManaBrewEngineAdapter {
         }
         final ManaBrewInteractiveSession session =
                 new ManaBrewInteractiveSession(request.getGameId());
+        session.setStartingPlayerIndex(request.getStartingPlayerIndex());
         final List<RegisteredPlayer> registeredPlayers = new ArrayList<>();
         for (PlayerConfig playerConfig : request.getPlayers()) {
             Deck deck = buildDeck(playerConfig);
             RegisteredPlayer registeredPlayer = RegisteredPlayer.forVariants(
                     playerCount, variants, deck, null, false, null, null);
+            // The host's format configuration is authoritative (for example,
+            // Duel Commander uses the Commander rules with 20 starting life).
+            registeredPlayer.setStartingLife(request.getStartingLife());
             if (playerConfig.isAi()) {
                 registeredPlayer.setPlayer(new LobbyPlayerAi(playerConfig.getName(), null));
             } else {
@@ -381,7 +385,10 @@ public final class ManaBrewEngineAdapter {
                     && playerObject.get("ai").getAsBoolean();
             players.add(new PlayerConfig(name, deck, commanderNames, ai));
         }
-        return new StartGameRequest(gameId, variant, startingLife, seed, players);
+        Integer startingPlayerIndex = root.has("startingPlayerIndex")
+                && !root.get("startingPlayerIndex").isJsonNull()
+                ? root.get("startingPlayerIndex").getAsInt() : null;
+        return new StartGameRequest(gameId, variant, startingLife, seed, players, startingPlayerIndex);
     }
 
     private static String requiredString(final JsonObject object, final String key) {
@@ -405,6 +412,7 @@ public final class ManaBrewEngineAdapter {
         private final int startingLife;
         private final long seed;
         private final List<PlayerConfig> players;
+        private final Integer startingPlayerIndex;
 
         public StartGameRequest(
                 final String gameId,
@@ -412,6 +420,13 @@ public final class ManaBrewEngineAdapter {
                 final int startingLife,
                 final long seed,
                 final List<PlayerConfig> players
+        ) {
+            this(gameId, variant, startingLife, seed, players, null);
+        }
+
+        public StartGameRequest(
+                final String gameId, final String variant, final int startingLife,
+                final long seed, final List<PlayerConfig> players, final Integer startingPlayerIndex
         ) {
             if (gameId == null || gameId.isBlank()) {
                 throw new IllegalArgumentException("gameId is required");
@@ -419,11 +434,16 @@ public final class ManaBrewEngineAdapter {
             if (players == null || players.size() < 2) {
                 throw new IllegalArgumentException("at least two players are required");
             }
+            if (startingPlayerIndex != null
+                    && (startingPlayerIndex < 0 || startingPlayerIndex >= players.size())) {
+                throw new IllegalArgumentException("startingPlayerIndex is outside the registered players");
+            }
             this.gameId = gameId;
             this.variant = variant;
             this.startingLife = startingLife;
             this.seed = seed;
             this.players = List.copyOf(players);
+            this.startingPlayerIndex = startingPlayerIndex;
         }
 
         public String getGameId() {
@@ -445,6 +465,10 @@ public final class ManaBrewEngineAdapter {
         public List<PlayerConfig> getPlayers() {
             return players;
         }
+
+        public Integer getStartingPlayerIndex() {
+            return startingPlayerIndex;
+        }
     }
 
     public static final class PlayerConfig {
diff --git a/forge-harness/src/main/java/forge/harness/host/ManaBrewInteractiveSession.java b/forge-harness/src/main/java/forge/harness/host/ManaBrewInteractiveSession.java
index af6888b2d13ce862a40d5024695dd7b50ae1739d..38b12e69b2afd484e19b9a2f5f8d3925cb67ea99 100644
--- a/forge-harness/src/main/java/forge/harness/host/ManaBrewInteractiveSession.java
+++ b/forge-harness/src/main/java/forge/harness/host/ManaBrewInteractiveSession.java
@@ -52,13 +52,23 @@ public final class ManaBrewInteractiveSession {
     private volatile String latestPromptJson;
     private volatile int promptedPlayerIndex = -1;
     private long promptSeq;
+    private Integer startingPlayerIndex;
     private volatile boolean closed;
+    private volatile boolean closeRequested;
     private volatile Thread gameThread;
     private static volatile InteractiveBridge bridge;
     private volatile SpellAbility castingAbility;
     private final InteractiveSnapshotExtractor.SecretChoiceVisibility secretChoiceVisibility =
             new InteractiveSnapshotExtractor.SecretChoiceVisibility();
 
+    // Forge's mutable game graph belongs to the game thread. RPC readers must
+    // only observe complete, viewer-redacted projections from a decision boundary.
+    private record PublishedState(Map<Integer, String> snapshots, String prompt,
+                                  boolean gameOver, boolean failed) {}
+
+    private volatile PublishedState publishedState =
+            new PublishedState(Map.of(), null, false, false);
+
     ManaBrewInteractiveSession(final String sessionId) {
         this.sessionId = Objects.requireNonNull(sessionId, "sessionId");
     }
@@ -66,12 +76,19 @@ public final class ManaBrewInteractiveSession {
     void attach(final Match match, final Game game) {
         this.match = Objects.requireNonNull(match, "match");
         this.game = Objects.requireNonNull(game, "game");
+        publishState(); // Still single-threaded, before start().
     }
 
     public String getSessionId() {
         return sessionId;
     }
 
+    void setStartingPlayerIndex(final Integer index) {
+        // Set only before the game thread starts. The room host owns BO3
+        // previous-loser and explicit restart policy, not a client prompt.
+        startingPlayerIndex = index;
+    }
+
     public static void setBridge(final InteractiveBridge value) {
         bridge = value;
     }
@@ -85,38 +102,39 @@ public final class ManaBrewInteractiveSession {
         requireAttached();
         Objects.requireNonNull(rng, "rng");
         if (bridge != null) {
-            forge.util.MyRandom.setRandom(rng);
-            try {
-                match.startGame(game);
-            } catch (RuntimeException error) {
-                System.err.println("[mana-brew] interactive game error: " + error.getMessage());
-                error.printStackTrace(System.err);
-            }
+            gameThread = Thread.currentThread();
+            runGame(rng);
             return;
         }
-        gameThread = new Thread(() -> {
-            forge.util.MyRandom.setRandom(rng);
-            try {
-                match.startGame(game);
-            } catch (RuntimeException error) {
-                System.err.println("[mana-brew] interactive game error: " + error.getMessage());
-                error.printStackTrace(System.err);
-            }
-        }, "mana-brew-forge-" + sessionId);
+        gameThread = new Thread(() -> runGame(rng), "mana-brew-forge-" + sessionId);
         gameThread.setDaemon(true);
         gameThread.start();
     }
 
+    private void runGame(final Random rng) {
+        try {
+            forge.util.MyRandom.setRandom(rng);
+            match.startGame(game);
+            if (!game.isGameOver() && !closed) {
+                throw new IllegalStateException("interactive game ended without an outcome");
+            }
+            publishState();
+        } catch (RuntimeException | Error error) {
+            // A stable failure must replace the old prompt, otherwise clients
+            // keep waiting for a game thread that has already died.
+            publishedState = new PublishedState(Map.of(), null, false, true);
+            System.err.println("[mana-brew] interactive game error: " + error.getMessage());
+            error.printStackTrace(System.err);
+        }
+    }
+
     public void close() {
-        closed = true;
+        closeRequested = true;
         JsonObject action = new JsonObject();
         action.addProperty("kind", "pass");
         actions.offer(action);
-        if (game != null && !game.isGameOver()) {
-            game.setGameOver(forge.game.GameEndReason.Draw);
-        }
         final Thread thread = gameThread;
-        if (thread != null) {
+        if (thread != null && thread != Thread.currentThread()) {
             try {
                 thread.join(5000);
             } catch (InterruptedException interrupted) {
@@ -126,13 +144,35 @@ public final class ManaBrewInteractiveSession {
     }
 
     public String getLatestPromptJson() {
-        return latestPromptJson;
+        return readableState().prompt();
     }
 
     public String getSnapshotJson(final int viewer) {
         requireAttached();
-        return InteractiveSnapshotExtractor.snapshotJson(
-                game, castingAbility, sessionId, viewer, secretChoiceVisibility);
+        final String snapshot = readableState().snapshots().get(viewer);
+        if (snapshot == null) {
+            throw new IllegalArgumentException("invalid snapshot viewer: " + viewer);
+        }
+        return snapshot;
+    }
+
+    private PublishedState readableState() {
+        final PublishedState state = publishedState;
+        if (state.failed()) {
+            throw new IllegalStateException("interactive game failed");
+        }
+        return state;
+    }
+
+    private void publishState() {
+        final Map<Integer, String> snapshots = new LinkedHashMap<>();
+        for (int viewer = -1; viewer < game.getRegisteredPlayers().size(); viewer++) {
+            snapshots.put(viewer, InteractiveSnapshotExtractor.snapshotJson(
+                    game, castingAbility, sessionId, viewer, secretChoiceVisibility));
+        }
+        final boolean gameOver = game.isGameOver();
+        publishedState = new PublishedState(Map.copyOf(snapshots),
+                gameOver || closed ? null : latestPromptJson, gameOver, false);
     }
 
     void rememberSecretNumberViewer(final String sourceCardId, final Player viewer) {
@@ -156,7 +196,7 @@ public final class ManaBrewInteractiveSession {
     }
 
     public boolean isGameOver() {
-        return game != null && game.isGameOver();
+        return readableState().gameOver();
     }
 
     boolean isClosed() {
@@ -164,7 +204,8 @@ public final class ManaBrewInteractiveSession {
     }
 
     public String submitAction(final String actionJson) {
-        if (closed) {
+        readableState();
+        if (closed || closeRequested) {
             throw new IllegalStateException("session is closed");
         }
         final JsonObject canonical = JsonParser.parseString(actionJson).getAsJsonObject();
@@ -732,6 +773,11 @@ public final class ManaBrewInteractiveSession {
 
     Player awaitFirstPlayerRoll(final int playerId, final List<Player> players) {
         requireAttached();
+        if (startingPlayerIndex != null) {
+            final Player first = game.getRegisteredPlayers().get(startingPlayerIndex);
+            game.setStartingPlayer(first);
+            return first;
+        }
         final Random rng = forge.util.MyRandom.getRandom();
         final int sides = 20;
         final Map<Player, Integer> openingRolls = new LinkedHashMap<Player, Integer>();
@@ -762,6 +808,7 @@ public final class ManaBrewInteractiveSession {
             }
             contenders = top;
         }
+        game.setStartingPlayer(winner);
         publishFirstPlayerRollPrompt(playerId, players, openingRolls, winner, sides);
         awaitFirstPlayerRollAcknowledgement();
         return winner;
@@ -1824,7 +1871,19 @@ public final class ManaBrewInteractiveSession {
     }
 
     private JsonObject takeAction() throws InterruptedException {
+        boolean needsPublication = true;
         while (true) {
+            if (closeRequested) {
+                closed = true;
+                if (!game.isGameOver()) {
+                    game.setGameOver(forge.game.GameEndReason.Draw);
+                }
+                return syntheticPass();
+            }
+            if (needsPublication) {
+                publishState();
+                needsPublication = false;
+            }
             if (bridge != null && actions.isEmpty() && !closed && !game.isGameOver()) {
                 submitAction(bridge.exchange(promptedPlayerIndex, latestPromptJson));
             }
@@ -1844,6 +1903,9 @@ public final class ManaBrewInteractiveSession {
                     : promptedPlayerIndex;
             concedePlayer(target);
             if (target != promptedPlayerIndex && !gameDecided()) {
+                // A different seat may concede while this same prompt remains
+                // active. Publish the completed transition before waiting again.
+                needsPublication = true;
                 continue;
             }
             return syntheticPass();
@@ -1901,7 +1963,7 @@ public final class ManaBrewInteractiveSession {
             if (host == null) {
                 continue;
             }
-            final String label = formatActionLabel(labels.get(i));
+            final String label = priorityActionLabel(sa, formatActionLabel(labels.get(i)));
             if (label.isEmpty()) {
                 continue;
             }
@@ -1933,6 +1995,10 @@ public final class ManaBrewInteractiveSession {
         publishAgentPrompt("player-" + playerId, null, new ChooseActionInput(actionsArray));
     }
 
+    private static String priorityActionLabel(final SpellAbility sa, final String label) {
+        return label.isEmpty() ? "" : label + (sa.isCastFaceDown() ? " (Face down)" : "");
+    }
+
     private static String abilityDescription(final SpellAbility sa, final String fallback) {
         final String text = sa.toString().trim();
         return text.isEmpty() ? fallback : text;
diff --git a/pom.xml b/pom.xml
index a5346a22b3e90e23b7bb00a0994f6bd1ff8d3feb..3a141cf47f35c5d4e3d9fc5793807da3ecf3f568 100644
--- a/pom.xml
+++ b/pom.xml
@@ -24,6 +24,7 @@
 
     <modules>
         <module>forge</module>
+        <module>target/hexproof-xmlpull-api</module>
         <module>forge-harness</module>
     </modules>
 
